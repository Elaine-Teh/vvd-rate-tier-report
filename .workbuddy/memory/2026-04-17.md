# 2026-04-17 工作记录

## Rate Tier Report 迭代
- 继续优化 `generate_combined_report.py` 生成三港合并报告 `CNSHA_CNSHK_CNNAS_Rate_Tier_Report.html`
- 修复港口代码显示：第三港应为 **CNNAS**（宁波），之前误显示为 CNSNAS/NS
- Thresholds 标签页重构为**垂直堆叠**布局：每个港口独占一整行宽表格，避免横向滚动
- 表格包含完整 FAK / T2 / T1 / Spot 四个等级，20' 和 40' 均显示
- 用色编码：🟢 FAK 绿、🟡 T2 黄、🔵 T1 蓝、🔴 Spot 红
- **Report Tab 页面布局**：
  - **Tier Distribution**：改为按 **TEU 计量**，Total 列显示 TEU + 百分比（如 `393 (39.7%)`）
  - **T1 Bookings tab** → 改为 **T1 Shipper tab**，按 Shipper Name Top 10 by TEU
  - **Spot tab**：只显示 CNSHK 和 CNNAS，B/L 明细，空则留空不显示文字
  - 港口副标题：CNSHK → **"Shenzhen"**，CNNAS → **"Nansha"**
- **Freight Rate tab**：
  - 数据源：`INCOME_BKD_MARKETING` sheet
  - **正确公式**：Sum(charges) / Sum(TEUs) per POD
  - **按 20' / 40' 分开显示**：每列显示 POD / 20' TEU / 20' Rate / 40' TEU / 40' Rate
  - **关键修复**：空箱（FE20/FE40）有 freight 费用但没有 OFT 行 → OFT_RateAs=0 → TEU=0
  - 分子：包含所有 freight（OFT+LSS+WRS+BAF）包括空箱
  - 分母：只包含有 OFT 的 loaded 箱（空箱不计入 TEU）
  - **避免混合箱型归类错误**：需按 (B/L, Container Type) 分组，不能只按 B/L
