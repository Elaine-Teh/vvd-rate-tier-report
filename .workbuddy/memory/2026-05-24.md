# 2026-05-24 工作记录

## AVG Contribution 整合到 VVD Tier Analyzer (12:36-14:00)

**需求**: 链接 `Master Data - Elaine/AVG Contribution By Vessel.xlsx` 到 VVD Tier Analyzer，按 POR-DEL 港口对展示 CM/TEU (20') 和 CM/FEU (40')。

**数据字段**: Revenue Month | Lane | VVD | SUL_YN | POR | POL | POD | DEL | 20GP(TEU, CM, PER TEU CM) | 40HC(TEU, CM, PER TEU CM)

**改动** (vvd_tier_analyzer.py):
1. CONFIG: 新增 `AVG_CONTRIB_FILE` 和 `LOCAL_AVG` / `FALLBACK_AVG` 路径
2. `sftp_download()`: 下载第三个文件，SFTP 不可用时优先使用本地文件，AVG 缺失时 gracefully skip
3. 新增 `parse_avg_contribution()`: 解析 Excel，按 VVD+POR+DEL 分组，40' CM/TEU → CM/FEU (×2)
4. `process_vvd()`: 接收 avg_data 参数，将对应 VVD 的 POR-DEL 贡献数据附加到 result
5. `generate_html()`: 新增 "Avg Cont" tab（仅当 VVD 有贡献数据时显示），在 Thresholds 和 Vessel Cost 之间
6. JS: `renderAvgContributionPanel()` 渲染 POR | DEL | 20' CM/TEU | 40' CM/FEU 表格 + 加权平均行

**当前状态**: AVG Contribution 文件不在本地（SFTP 连不上），Tab 逻辑已就绪。文件到位后自动激活。

**HTML 大小**: 4.79 MB (492 VVDs, 32872 B/Ls, 136584 TEU)

**未推 GitHub**（用户明确要求等待 permission）。
